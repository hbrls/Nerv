# TASK-001 · 下一轮 LENS 任务模板

> LENS：`{{LENS_PATH}}`
> 主题：`{{TOPIC}}`

{{RULE_BLOCK}}

## 背景

本任务由 pilot 在上一轮收束后创建，用于补齐当前讨论仍缺少的增量。

本模板编号表示相对顺序。落地时，若当前 pilot TASK 编号为 `NNN`，本模板应生成当前 Vision 目录内的 `TASK-(NNN+1).md`，输出写入当前 Vision 目录内的 `C-(NNN+1).md`。

## 输入

读取以下文件或文件集合：

- `{{INPUT_1}}`
- `{{INPUT_2}}`

## 目标

围绕 `{{FOCUS}}` 完成本轮分析，产出 `{{EXPECTED_INCREMENT}}`。

`{{FOCUS}}` 和 `{{EXPECTED_INCREMENT}}` 只能来自当前规则块或既有任务契约授权的缺口描述。不得把前序 TASK 的结论写成对本 TASK 的执行预期，不得为未来 TASK 预设研究方法、优先级、隐性评价标准、结论或执行路径。

## 约束

- 只读取本任务输入中列出的文件
- 不读取其它 `TASK-*`
- 不读取或写入其他 Vision Id 目录
- 不创建、修改或删除除指定输出文件以外的任何文件
- 不承担结束或继续讨论的调度职责
- 只对本任务的契约与输出负责，不绑定前序 TASK 的执行预期
- 如发现规则不足，只能在输出中建议更新任务契约或规则源，不得自行扩展本任务的判断标准

## 输出

将完整输出写入 `{{OUTPUT_C_PATH}}`。
