# C-005 · C-003 三处「未在本轮锁定」的闭合尝试

> 对应任务：TASK-005（VISION-003）
> 输入文件：`.context/VISION-003/Index.md`、`.context/VISION-003/C-001.md`、`.context/VISION-003/C-003.md`
> 输出契约：闭合 C-003 §2.1 / §2.2 / §2.3 中三处「未在本轮锁定」内容，每处至少一句话锁定结果，或显式标记「需用户回答」并列出具体问题

## 0. FOCUS

仅处理 C-003 §2.1 / §2.2 / §2.3 中已显式标注「未在本轮锁定」的三处内容：

1. **§2.1**：读取对象的具体子集（blames / 仓库源码 / MR / Issue / Pipeline）
2. **§2.2**：下游能力的具体接入点（catalog 同步 / techdocs / search）
3. **§2.3**：成功标准中每一条的可执行检查方式与阈值

不在本轮范围：中阻塞 / 低阻塞问题（与 Gitea 关系 / VISION-002 完成判据 / 鉴权与网络约束）。

## 1. EXPECTED_INCREMENT

本轮产物 = 本文件（C-005.md），包含三处「未在本轮锁定」内容的闭合尝试或「需用户回答」标记，附所需的具体问题。不向下一轮注入研究方法、优先级、隐性评价标准。

## 2. 闭合尝试结果

### 2.1 §2.1 · 读取对象的具体子集

**锁定结论**：**需用户回答**。

**理由**：本任务可读取的三个输入文件（Index.md / C-001.md / C-003.md）均未提供具体子集的偏好或证据。C-003 §2.1 仅把范围收敛到「backstage.js 数据源扩展」，未触及 blames / 仓库源码 / MR / Issue / Pipeline 之间的取舍。按 Index.md 用户约束，实际项目不在 Nerv，本环境无法独立判断。

**所需用户回答的具体问题**：

- Q1.1：Gitlab 读取的**首要对象**是什么？候选：仓库源码 / MR（含 diff 与评论）/ Issue / Pipeline 日志 / blames（行级溯源）/ 其它。
- Q1.2：该对象的**读取用途**是什么？候选：作为 catalog 实体元数据 / 喂给 techdocs 构建 / 喂给 search 索引 / 直接展示给开发者 / 其它。
- Q1.3：是否存在**次要对象**（与首要对象一并读取但优先级更低）？如有，请列出。
- Q1.4：该对象是否需要**历史回溯**（如历史 MR / 跨分支 blames），还是仅最新状态即可？

### 2.2 §2.2 · 下游能力的具体接入点

**锁定结论**：**需用户回答**。

**理由**：C-003 §2.2 把范围收敛到「backstage.js 内部能力链路（候选：catalog 同步 / techdocs / search）」，但未收敛到具体一个。Index.md 与 C-001.md 均未提供偏好；本环境无法独立判断。

**所需用户回答的具体问题**：

- Q2.1：Gitlab 读到的数据**首要接入**到哪个能力？单选：catalog 同步 / techdocs / search / 其它（请注明）。
- Q2.2：该接入是否需要**与现有 Gitea 数据源并存**（按项目类型路由），还是 Gitlab 完全替换 Gitea？
- Q2.3：是否需要**复合接入**（同一份数据被多个下游消费）？如是，请列出下游列表与各自用途。
- Q2.4：是否存在**反向链路**（backstage.js 写回 Gitlab，如创建 MR / 评论）？本轮「读取」是否仅指只读？

### 2.3 §2.3 · 成功标准的具体阈值

**锁定结论**：**需用户回答**。

**理由**：C-003 §2.3 把标准收敛到三条 AND（能拉到 / 能消费 / 能扛住），但未给出每条的可执行检查方式与阈值。本环境无 Gitlab 实例信息（self-hosted vs SaaS、token、网络可达性），无法独立设定。

**所需用户回答的具体问题**：

- Q3.1（针对「能拉到」）：**验收用例**是什么？请给出一个或多个可手工 / 自动验证的具体场景（如：拉取指定 MR 的 diff、返回非空且字段完整）。
- Q3.2（针对「能拉到」）：**字段完整性**判据是什么？候选字段集至少包括哪些？
- Q3.3（针对「能消费」）：**端到端贯通**的判据是什么？如：在 backstage 页面看到由 Gitlab 数据驱动的 catalog 项 / techdocs 构建成功 / 搜索命中。
- Q3.4（针对「能扛住」）：Gitlab 实例是 **self-hosted 还是 SaaS**？对应 API rate limit 上限与本任务允许的余量是多少？
- Q3.5（针对「能扛住」）：可接受的 **P95 延迟**上限是多少（如单次读取 < 2s）？是否包含分页拉取的全量时间？
- Q3.6（针对「能扛住」）：**鉴权方式**是什么（Personal Access Token / Project Token / OAuth）？token 的轮换策略与失效兜底？

## 3. 源约束缺口回写建议

- 本轮三处「未在本轮锁定」按 C-001 §7 明示要求仍属进入分析 / 方案 / 代码阶段前的**强制闭合项**；本轮尝试闭合失败（受限于本环境无法访问用户 / 实际项目），故下游任务契约应明确要求**显式采集用户回答**后再进入下一阶段。
- 建议把 §2 中 14 个具体问题（Q1.1–Q1.4 / Q2.1–Q2.4 / Q3.1–Q3.6）作为下一轮 LENS 向用户提交的最小问题集；不要在未获得这些回答前收敛任何方案。
- 本轮未涉及 C-001 §8 提议的两条 Index.md 回写（C-001 §8.1 / §8.2），仍按 C-004 判定不在本轮范围内，保留为下游缺口。

## 4. 元信息

- 尝试闭合的三处中，**全部标记为「需用户回答」**；未出现可独立锁定的子项。
- 本文件不替代下游用户交互；下游应在拿到 §2 中的具体问题回答后重跑本任务契约，方可进入下一阶段。